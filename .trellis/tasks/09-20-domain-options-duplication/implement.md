# 执行计划

## 前置
- [x] 用户批准创建任务和规划，已读取归档记录与实际调用链。
- [x] PRD、设计、证据和上下文清单落盘。
- [x] 任务已由用户启动；第一阶段提交 dc6b7f64，用户授权本会话完成第二阶段。
- [x] 第二阶段加载 trellis-before-dev 与对应脚本/质量规范，主会话派发 implement/check 子代理；本阶段不修改 React 组件。

## 实施分组
1. 候选领域化：实现代理负责候选 features、基础类型、调用方和测试；调整 mock，修复币种迟到失败；不触碰扫描器。实现后补币种竞态和各缓存遗漏场景。运行迁移测试、组织切换/退出、订单与财务受影响测试、tsc、修改文件 Biome、check:architecture 和 diff 检查后提交。
2. 重复发现：实现代理负责根/web 脚本、Go 辅助工具与 package 检查入口。实现后添加检测正反例、作用域/闭包/属性差异、排除和稳定输出测试。不得提前写测试做 TDD。分别运行 Node 与 Go 工具测试、真实源码扫描；记录实际覆盖、耗时和错误，避免大量候选截断后声称全量审查。
3. 人工审阅和导航：主会话审查前十组（不足十组全部），保存报告和建议，更新能力导航与指南；检查入口链接和 diff 后提交。

## 质量门禁
- check 子代理复核迁移语义、测试覆盖和扫描误报/漏报边界。
- 完整 pnpm run check:web；Go 辅助工具单测；pnpm run report:duplicates 验证文本和 JSON 均成功。
- 本任务不改业务后端、构建依赖或路由入口，默认不做服务端全量业务测试和生产构建；若实际改动扩大，再据风险追加。
- 每组 Conventional Commit 中文提交，完成后按 finish-work 归档并记录成果与限制，不自行推送。

## 第二阶段执行记录

- 范围：重复扫描器、工具测试和检查入口；同时记录本次扫描审查结果与使用规范，形成可验收交付。
- 第一阶段产品迁移已存在，本会话不重做；能力导航原有旧 options 入口改为实际领域入口。
- 开始时 task.json 已有用户启动任务的未提交变更，保留其状态。

## 第二阶段验收结果（2026-09-20）

- 新增按需 `report:duplicates` 命令、前端 Babel AST 适配器与独立 Go 标准库辅助模块；不修改产品业务实现。
- 专项测试：Node 14、Go 7 用例通过；独立 check 审查通过，修复自递归/默认参数、计算键、switch、Go 展开参数/类型别名与注释计数误报。
- 完整 `pnpm run check:web` 退出 0：160 个测试文件通过、1 跳过；884 用例通过、12 跳过。权限/枚举、架构、Biome 与 tsc 均通过。日志存在 lint 提示和 localhost:3000 连接错误输出，具体网络噪音来源未确认，不宣称零警告。
- `git diff --check`、新脚本 Biome 与 Go vet 通过；本轮未修改路由/构建或后端业务，未运行生产构建与服务端全量业务测试。
- 实仓扫描覆盖 871 个文件、7631 个函数，8 组候选、0 错误；文本/JSON 重复运行字节稳定。所有 8 组已人工审阅，详见 research/duplicate-review.md 与 duplicate-report.json。
- 前端工具测试进入 check:web；Go 测试进入 check:server，并显式接入现有 CI server job。
- 更新候选能力导航、扫描规范与规范索引，完成原计划第三组的使用说明和报告审阅，避免工具缺少可复核成果。
- 后续可按报告处理确认的业务重复；本任务不自动合并这些实现，也不承诺跨文件类型解析或跨语言语义等价。

- 工具实现提交：`3cc5722a`。
