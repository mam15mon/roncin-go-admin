# 竞品 37 项费用表头对照

依据：用户提供的「表头排序」截图，仅能证明字段可配置，不能证明竞品的计算、权限或锁定规则。核对时间：2026-09-22。当前工作区包含其他会话的订单与费用未提交改动；本研究按工作区现状记录，执行前重新核对。

## 证据索引

- E1：`server/api/order/v1/order_fee.proto:165`，OrderFee 已有费用、税额、汇率快照、本币金额、版本与标签；没有独立费用业务编号、创建人、账单／发票投影。
- E2：`web/src/pages/orders/components/fees/OrderFeeTableTabs.tsx:419`，行内录入列；`:374` 保存请求固定 `taxInclusive: true`；应收应付纵向双表、request/actionRef、无列偏好接入。
- E3：`server/internal/biz/order_fee.go:420`，税额及本币计算使用 Decimal，保存后的后端结果为权威。
- E4：`server/api/finance/v1/settlement.proto:312`，FeeLedgerItem 已有 bill_no、financial_progress、finance_locked、组织名称；没有 invoice_no、verified_amount、unverified_amount。
- E5：`server/internal/data/settlement.go:153`，活动账单行及非取消账单、已开票且有效关联、有效核销与已确认对冲共同决定投影；`:185` 计算的是关联账单整体进度。
- E6：`server/internal/data/ent/schema/finance_bill_line.go:52`，活动费用行的部分唯一索引确保一条费用最多对应一条活动账单行；历史账单不等于当前活动账单。
- E7：`web/src/pages/finance/fees/components/feeLedgerColumns.tsx:250` 发票列无契约字段支撑；`:497` 已核销／未核销金额明确渲染占位。不能把列名存在认作能力已完成。
- E8：`web/src/components/ui/finance-ledger-template/TableColumnConfigModal.tsx:1`，现有配置窗耦合 153 项财务字段、台账请求、分页与颜色。`server/internal/data/ent/schema/finance_fee_ledger_preference.go:20` 偏好按组织与用户隔离，不能直接用同一记录保存订单列。
- E9：`web/src/pages/orders/fees.tsx:196`，页面写禁用合并业务锁策略和财务锁；`order-fee-panel-columns.tsx:33` 抽屉已有标签列。
- E10：`server/api/finance/v1/settlement.proto:20`，订单维度财务明细需要 system.finance.fee.read；订单费用接口使用订单 fee.read，二者不可互换。
- E11：`web/config/routes.ts:178`，已有 `/finance/fees/detail/:orderId` 订单财务详情入口。
- E12：`.trellis/spec/server/backend/finance-bill-currency.md`，普通账单固定费用币种，费用日汇率与账单日汇率分别保存；核销、对冲有效口径不同于对账。

## 全量对照

状态：「已有展示」指订单主费用表；「已有数据」不代表该表已有列；「关联可用」指有授权的财务接口已提供；「缺口」或「待定义」不进入本期列选择器。

| 序号 | 竞品字段 | 我们的现状与依据 | 本期建议 |
|---|---|---|---|
| 1 | 费用编号 | 有 id 和费用项目 feeCode；后者不是费用行编号（E1） | 展示现有费用代码，不新增流水编号 |
| 2 | 费用名称 | feeName 已有展示（E1/E2） | 默认、录入必备 |
| 3 | 结算单位 | settlementPartyName 已有展示（E1/E2） | 默认、录入必备 |
| 4 | 计费单位 | billingUnit 已有展示（E1/E2） | 默认、录入必备 |
| 5 | 数量 | quantity 已有展示（E1/E2） | 默认、录入必备 |
| 6 | 币种 | currency 已有展示（E1/E2） | 默认、录入必备 |
| 7 | 汇率 | exchangeRate 及来源、日期快照已存在（E1） | 默认，明确费用日口径 |
| 8 | 单价 | unitPrice 已有展示（E1/E2） | 默认，明确含税标识 |
| 9 | 总价 | totalAmount 为后端含税总金额（E3） | 默认，保持后端权威 |
| 10 | 税率(%) | taxRate 已在费用快照（E1/E3） | 可选只读列，不新增改税率权限 |
| 11 | 税金 | taxAmount 已计算（E3） | 可选只读列 |
| 12 | 不含税单价 | 没有独立契约，含税标志影响输入单价语义（E1/E3） | 暂缓，不用总额除数量临时拼出新口径 |
| 13 | 折本币总价 | baseCurrencyAmount + baseCurrency 已有（E1） | 可选只读列，展示本币代码 |
| 14 | 不含税总价 | netAmount 已有（E1/E3） | 可选只读列 |
| 15 | 费用状态 | 草稿／已确认／已建账／已作废（E1） | 默认，与账单进度分开 |
| 16 | 费用时间 | expenseDate 为纯日期（E1/E2） | 保留「发生日期」，不冒充创建时间 |
| 17 | 是否对账 | 现有财务进度不是客户对账确认（E4/E5） | 暂缓独立对账状态 |
| 18 | 是否锁定 | 有订单业务锁与提成净额财务锁（E9） | 页面解释现有禁用原因，不命名单条费用锁 |
| 19 | 账单号 | 财务接口有当前活动 billNo（E4/E6） | 有财务读取权限时可选；提供订单财务详情入口 |
| 20 | 发票号 | 发票实体有号码；费用响应没有映射（E4/E7） | 后续关联投影，支持多发票与红冲历史 |
| 21 | 备注 | note 已有展示（E1/E2） | 默认 |
| 22 | 关联信息 | 竞品含义不明，不能假设为订单或提单（截图） | 暂缓，已有订单上下文不重复造字段 |
| 23 | 审核状态 | 有费用确认和补录审批，不是通用费用审核（E1） | 复用原流程，不新增审核状态机 |
| 24 | 结算币种 | 普通账单沿用费用币种（E12） | 暂缓重复列，不开放跨币种改写 |
| 25 | 结算汇率 | 账单日汇率不同于费用日汇率（E12） | 留在账单详情，不用费用汇率替代 |
| 26 | 结算金额 | 核销分摊到达账单，费用台账无费用级金额（E4/E7） | 暂缓；不能把整账单金额复制到每条费用 |
| 27 | 开票时间 | 发票有 invoiceDate 与 issuedAt，费用列表无聚合（E4） | 与发票投影后续一起定义 |
| 28 | 佣金比例(%) | 提成已有独立方案、归属及计提链路（领域地图） | 不新增费用行佣金计算 |
| 29 | 单条费用锁定时间 | 没有该独立锁模型（E1/E9） | 暂缓，不套订单锁时间 |
| 30 | 单条费用锁定人 | 没有该独立锁模型（E1/E9） | 暂缓 |
| 31 | 应收/应付费用锁定时间 | 没有同名方向锁模型（E9） | 暂缓 |
| 32 | 应收/应付费用锁定人 | 没有同名方向锁模型（E9） | 暂缓 |
| 33 | VAT | 截图不能说明与税率／税金区别 | 暂缓独立字段 |
| 34 | 费用创建人员 | 费用 schema 没有 created_by，created_at 不等于创建人（E1） | 后续按真实创建写路径记录；不拿操作员冒充 |
| 35 | 费用标签 | tags 已有；抽屉已展示（E1/E9） | 主费用表增加可选只读列，沿用已有标签管理 |
| 36 | 所属公司 | 订单上下文与台账 organizationName 已有（E4） | 订单页保留上下文，不在每行重复 |
| 37 | 标记入账月份 | 没有对应费用契约（E1） | 暂缓，不从费用日期自动推导会计月份 |

## 额外值得展示的现有信息

- 「关联账单财务进度」：复用现有投影，名称与说明明确为整张关联账单的进度，不能写成单条费用的实际收款进度。
- 费用创建时间／更新时间已存在；非本期必要默认列，后续按本地时区格式化。

## 历史与证据限制

`trellis mem search '费用 行内'` 命中 zcode 会话 `sess_3efd95b`，记录了行内汇率预览和可搜索下拉的近期工作。这里只用来识别并行变更，不把搜索片段当成新需求授权。当前行内汇率、计费单位与日期联动必须在执行前重新读取，禁止覆盖。
