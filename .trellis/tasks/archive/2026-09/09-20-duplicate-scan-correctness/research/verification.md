# 验证与交付

- 三项缺陷全部修复：TS运行时包装绑定、头部注释生成标记、严格基线可比性；同步修复扫描失败覆盖基线的相邻路径。
- Node 工具测试20项通过；Go辅助工具测试通过；修改文件Biome零问题；web tsc、git diff --check通过。
- 独立check代理只读复查通过；额外探针覆盖嵌套断言纯改名、局部/外部绑定、枚举exact-only和缺失基线配置。
- 同配置实仓扫描：前端466文件、Go411文件、7628函数、3组候选、errors为空；基线新增/消失/成员变化全部0。统计基于当时工作区，含并行工作台未提交代码，因此不把数量视作固定契约。
- 不同阈值基线CLI非零退出，不覆盖已有输出；扫描错误时writeBaseline不覆盖已有基线。
- 已提交基线重新生成，diff仅新增10行比较元数据，groups不变，没有纳入工作台候选。
- 验证按工具修复风险选取；未运行产品全量测试/构建或数据库操作。本轮不修改工作台或其他产品代码。
- 报告version升级2，不支持旧groups-only基线静默回退；使用baseline:duplicates生成新格式。
