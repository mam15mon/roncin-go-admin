# 订单面包屑统一实施计划

## 开始前检查

- [x] 用户批准本设计后再激活任务；设计请求本身不视为实施批准。
- [x] 检查工作区，记录并避开其他任务文件；已独立提交的 TagsView 规则不在本任务修改范围。
- [x] 读取 PRD、设计、现状审计和 `implement.jsonl` 引用规范。
- [x] 确认 `web/` 下不存在更近的 `AGENTS.md`。

## 第一组：订单公共页头与链接语义

- [x] 为 `OrderKindConfig` 增加明确的面包屑导航名称，不通过字符串替换推导。
- [x] 新增 `OrderPageHeader`，集中实现 create/detail/fees/split 的层级、title 和返回目标。
- [x] 补四种页面矩阵测试，断言每个上级链接和当前项。
- [x] 修正 `PageHeaderShell.href` 实现，并验证现有 `onClick` 调用兼容。

## 第二组：接入订单页面

- [x] 新建页替换手写 breadcrumbs，消除当前页重复。
- [x] 详情页使用公共页头；锁状态保留在页头标签区，业务操作栏原样保留在下方。
- [x] 费用页删除手写导航和右侧重复返回按钮，只保留刷新；摘要订单号改为普通文本。
- [x] 拆票页替换手写 breadcrumbs 与重复标题，保留底部取消返回。
- [x] 列表路由面包屑保持现状，只做回归确认。

## 第三组：异常状态与路由校验

- [x] 详情和费用在加载中、订单不存在时继续渲染公共页头。
- [x] 真实订单号未加载时使用路由 ID，加载后显示真实编号。
- [x] 移除详情和费用的海运出口静默兜底；无效 kind 进入明确错误状态。
- [x] 验证合法海运出口的请求参数、返回目标和权限行为不变。

## 第四组：验证与独立检查

优先运行：

```bash
pnpm --dir web test -- src/components/ui/page-shell src/pages/orders
pnpm --dir web tsc
pnpm --dir web biome:lint
git diff --check
```

- [x] 根据仓库脚本能力调整为等价 pnpm 定向命令，不使用 npm/npx，不跳过规则。
- [x] 独立 `trellis-check` 核对层级、链接、异常页、重复入口和业务功能回归。
- [x] 修正发现的问题并重跑相关测试。
- [x] 提交前逐文件确认本任务没有继续修改 TagsView，也不包含其他任务改动。

## 规范、提交与收尾

- [x] 将订单页面面包屑层级和“breadcrumbs 仅传上级、title 表示当前页”写入前端规范。
- [x] 提交信息：`fix(web): 统一订单页面面包屑导航`。
- [x] 完成 Trellis 检查、规范更新、归档和开发日志。
- [x] 未经用户另行要求不自动推送。

本任务可整体回退前端提交；不得回退或覆盖已独立完成的 TagsView 及其他任务工作。
