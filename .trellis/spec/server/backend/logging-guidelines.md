# 日志与审计规范

## 运行日志

- 结构化 JSON 输出（平台能力见 `internal/platform/`）。
- 记录足以定位问题的上下文（组织 ID、业务单号、错误原因），但不倾倒报文。

## 审计日志

- 业务审计独立存储，与运行日志分开。
- 敏感操作（确认、支付、取消、导出等）记录操作人、组织、关键参数与结果。
- 范例：提成导出审计记录操作人、组织、筛选条件、导出行数与成功/失败，但不
  保存完整导出内容。

## 禁止记录的内容

密码、Cookie、令牌、完整敏感请求/响应报文。提成等涉及员工收入的数据，导出
比普通查询更敏感，必须有独立权限与审计。
