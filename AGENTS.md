# Roncin Go Admin 开发约束

## 通用原则

- 所有文档与设计说明使用中文。
- 禁止 TDD；功能实现后按风险补充验证。
- 禁止在没有明确要求时添加自动回退或兼容旧数据的逻辑。
- 每次完成一组可验证修改后提交 Git，提交信息准确描述本组变化。
- 前端依赖与脚本统一使用 `pnpm`，禁止新增 `npm`、`npx`、Yarn 入口。

## 后端边界

- `api/` 是 HTTP/gRPC 契约真相源；生成代码不得手改。
- `internal/service` 只做 DTO 与领域对象转换、参数校验和用例调用。
- `internal/biz` 持有领域对象、用例、仓储接口与业务规则。
- `internal/data` 实现仓储和持久化转换，不向上泄漏 Ent 类型。
- `internal/server` 只负责传输层注册、中间件和静态资源服务。
- 权限码在后端 Manifest 中统一定义；前端只消费 `/api/v1/auth/me` 返回的权限集。
- 运行日志输出结构化 JSON；业务审计日志独立存储，禁止记录密码、Cookie、令牌或完整敏感报文。

## 前端边界

- 路由级权限使用 Ant Design Pro `access`；按钮级权限使用同一权限判断结果。
- 所有接口通过统一请求配置和 OpenAPI 生成客户端访问，禁止页面自行拼接后端地址。
- 服务端状态优先使用 React Query；不要将接口数据复制到全局可变状态。
- 新增页面按业务领域拆分，避免导入无关大组件和聚合导出。

## 数据与部署

- PostgreSQL Schema 以 Ent Schema 为真相源；生产变更必须生成并审阅迁移。
- 私密配置只通过环境变量注入，仓库只提交 `.env.example`。
- 生产采用同域部署：Go 提供 `/api/*`、`/health/*` 和 React 静态资源。

