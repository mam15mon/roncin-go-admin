# Roncin Go Admin 重构计划

## 目标与前提

`roncin-go-admin` 以 Go 服务端和 React 管理后台作为最终目标，`roncin` 作为现有业务行为、数据模型和验收场景的参考实现。

本计划默认不添加双写、旧接口兼容或静默回退逻辑。是否保留旧系统存量数据，需要在数据迁移阶段单独决策；如果允许冷启动，则不为旧数据增加隐式兼容层。

总体依赖顺序如下：

```text
基础设施
  ↓
认证 / 组织 / RBAC
  ↓
主数据 / 往来单位
  ↓
订单核心
  ↓
费用 / 账单 / 财务
  ↓
参数 / 模板 / 通知 / 集成
  ↓
数据迁移与正式切换
```

## 阶段计划

### P0：建立迁移基线

- 确认旧项目和新项目的系统边界、生产入口和最终责任归属。
- 建立旧 Prisma 模型、旧页面/API、新 Ent Schema、新 Proto 和新页面之间的迁移矩阵。
- 为每个领域标记“已完成、部分完成、未开始、明确不迁移”。
- 单独记录当前工作区改动，避免计划工作覆盖用户已有修改。

完成标准：形成经过评审的领域清单、依赖关系、暂不迁移清单和数据保留决策。

### P1：稳定新系统基础设施

- 固化 Kratos、Ent、Wire、PostgreSQL 和 React 管理后台的开发/构建入口。
- 完善统一错误响应、请求元数据、Trace、结构化日志和健康检查。
- 验证开发期前后端代理、生产同域静态资源托管和配置注入。
- 明确 Proto、Go 生成代码、OpenAPI 和前端客户端的生成链路。

完成标准：`pnpm run check`、Go 测试、Go vet 和生产构建可执行；登录前后的基础请求链路稳定。

### P2：完成认证与权限管理

- 完成用户、组织、成员关系、角色、权限、数据范围和会话管理。
- 完成登录、登出、当前用户、组织切换和会话失效。
- 增加用户管理、组织管理、角色管理和权限管理接口及页面。
- 保持后端权限校验为唯一安全边界，前端权限只用于菜单和按钮展示。

完成标准：能够完成“创建组织 → 创建用户 → 分配角色 → 登录 → 按权限访问”的完整链路，并验证组织隔离和默认拒绝规则。

### P3：完善审计体系

- 记录登录、登出、组织切换、用户/角色变更和业务写入操作。
- 完成审计日志查询接口和管理页面。
- 统一记录组织、用户、动作、资源、结果、请求 ID 和 Trace ID。
- 禁止将密码、Cookie、令牌和完整敏感请求/响应写入日志。

完成标准：关键安全和业务操作均可追溯，审计查询支持按组织、用户、动作和时间范围筛选。

### P4：迁移主数据

- 迁移币种、国家地区、机场、港口、城市、承运人和箱型等基础资料。
- 明确每类主数据的来源、唯一键、启停规则、导入方式和审计要求。
- 为订单表单提供统一的选项查询接口。

完成标准：订单表单所需基础选项均由新系统提供，主数据查询和维护不依赖旧系统。

### P5：完成往来单位模型

- 先决定 `Partner` 是否继续作为简化模型，还是扩展为旧 `Party` 的完整业务聚合。
- 映射客户、供应商、代理、联系人、账户、合同、附件、结算规则和黑名单等关系。
- 完成列表、详情、编辑、启停、导入、导出和权限控制。
- 对旧项目字段、状态、唯一性规则和操作行为逐项对齐。

完成标准：新模型与旧业务含义有明确映射；如果新系统有意缩减范围，必须记录为明确的业务决策，而不是隐式缺失。

### P6：迁移订单核心

- 迁移订单基础信息、业务类型、运输方式、贸易条款、服务类型和货物信息。
- 迁移订单列表、详情、创建、编辑、查询条件和基础校验。
- 迁移订单状态、里程碑和必要的操作审计。

完成标准：订单可以完成创建、编辑、查询和基础状态流转，关键校验结果与旧系统验收场景一致。

### P7：迁移订单执行能力

- 迁移集装箱、提单、附件、协作人员、异常处理和跟踪事件。
- 迁移提醒、告警、订单操作日志和相关后台任务。
- 对订单状态机和跨模块副作用进行集中封装，避免业务规则落入传输层。

完成标准：订单执行链路能够独立闭环，状态变更和异常处理可追溯、可重试。

### P8：迁移费用与账单

- 迁移费用录入、费用状态、应收应付、账单生成和账单状态。
- 对齐币种、税额、汇率、费用快照、利润和账单编号口径。
- 迁移费用和账单相关的权限、审计、导出和批量操作。

完成标准：费用到账单的金额和状态流转与旧系统关键场景逐项对齐。

### P9：迁移财务链路

- 迁移核销、收款、付款、发票、佣金和财务工作台。
- 迁移汇率快照、汇率审批和财务状态日志。
- 对金额计算、审批边界和财务审计进行专项验收。

完成标准：财务状态机、权限和金额来源完整可追溯，关键财务场景通过验收。

### P10：迁移参数、模板与通知

- 迁移字典、编号规则、状态模板、里程碑模板、页面模板和业务规则模板。
- 迁移通知、提醒规则、消息记录和用户通知偏好。
- 明确模板版本、发布、恢复和审计规则。

完成标准：新系统能够独立维护业务参数、模板和通知，不依赖旧系统运行时配置。

### P11：迁移集成任务与后台作业

- 迁移集成任务、任务日志、重试、死信、回放和任务追踪。
- 明确任务幂等键、租约、超时、失败告警和审计边界。
- 增加任务状态查询和运维检查能力。

完成标准：后台任务可观测、可重试、可审计，失败不会静默丢失。

### P12：数据迁移与切换

仅在确认需要保留旧系统存量数据时执行本阶段：

- 制定旧 Prisma 数据到新 Ent 数据的字段、枚举、关系和权限映射。
- 编写一次性迁移工具，先执行多轮离线演练和只读校验。
- 校验记录数量、金额、关系、组织归属、权限和关键业务状态。
- 制定数据冻结、正式迁移、失败处理和回滚步骤。

完成标准：迁移前后关键数据通过校验，正式切换有可执行的回滚方案；不引入长期双写逻辑，除非另行批准。

### P13：正式切换与旧系统下线

- 新系统先进行完整业务验收和必要的灰度观察。
- 冻结旧系统写入，完成最终数据同步或冷启动初始化。
- 新系统成为唯一生产入口，保留必要的只读观察期。
- 关闭旧系统生产写操作，并归档迁移报告、验收记录和运维手册。

完成标准：新系统独立承载生产业务，旧系统不再承担生产写入职责。

## 当前优先级

近期按以下顺序推进，不直接跳入订单页面开发：

1. 完成新旧领域迁移矩阵。
2. 决定 `Partner` 与旧 `Party` 的目标关系。
3. 补齐用户、组织、角色、权限和审计管理。
4. 完成主数据基础服务。
5. 完成往来单位第一版完整模型。
6. 再开始订单核心 API 和页面。

## 验证与提交约束

- 功能完成后按风险补充针对性验证，不采用先写测试再实现的 TDD 流程。
- 服务端优先运行 `go -C server test ./...` 和 `go -C server vet ./...`。
- 前端优先运行 `pnpm --dir web lint` 和 `pnpm --dir web test`。
- 修改 Proto、配置源或 Ent Schema 后，重新生成对应代码，不手工修改生成物。
- 每完成一组可验证的改动就独立提交，使用 Conventional Commits 前缀。
- 提交前确认没有秘密、调试输出、构建缓存或本地环境文件进入版本库。
