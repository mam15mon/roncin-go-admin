# Roncin Go Admin 重构计划

## 目标与前提

`roncin-go-admin` 以 Go 服务端和 React 管理后台作为最终目标，`roncin` 作为现有业务行为、数据模型和验收场景的参考实现。

本计划默认不添加双写、旧接口兼容或静默回退逻辑。是否保留旧系统存量数据，需要在数据迁移阶段单独决策；如果允许冷启动，则不为旧数据增加隐式兼容层。

总体依赖顺序如下：

```text
基础设施
  ↓
认证 / 组织 / RBAC
  ↓
审计基础能力
  ↓
主数据 + 参数/模板基础
  ↓
Partner/Party 决策 + 往来单位
  ↓
订单核心
  ↓
后台任务基础 + 订单执行
  ↓
费用 / 账单 / 汇率
  ↓
核销 / 收付款 / 发票 / 佣金
  ↓
通知 / 集成 / 运维
  ↓
数据迁移与正式切换
```

第一阶段上线范围建议控制为：认证、组织/RBAC、审计、主数据、往来单位和订单基础录入。订单执行、费用财务以及通知集成作为后续上线范围，避免所有领域同时开工却没有可验收闭环。

## 当前实施状态（2026-08-20）

- P2 已完成第一批可验收实现：Admin Proto、组织/用户/角色/权限目录接口、Ent 事务写入、权限中间件、管理审计和 `/admin` 手写 React 管理工作台。
- 组织管理已按当前组织边界收敛：列表只返回当前组织及直接子组织；新增组织自动挂到当前组织；根组织只允许通过冷启动初始化创建，避免管理员读取或修改全局组织树。
- 角色数据范围已进入后端授权判断，不能只在页面上展示后端不执行；组织管理要求 `all`，用户、角色、权限目录和往来单位要求当前组织及以上范围。角色权限与角色范围按角色码关联，避免多个角色组合时错误扩大权限。
- 权限目录保持后端 Manifest 只读，角色只保存授权关系。未引入低代码平台、拖拽式页面配置或生成式运行时，管理页面均为仓库内可审阅的 TypeScript/React 代码。
- P2 尚未宣告最终完成：组织树数据范围、角色停用策略和管理接口端到端验收仍是后续门禁；在这些内容完成前不把 P2 标记为全量完成。
- 密码重置已补齐为独立接口：新密码在 `biz` 层校验和哈希，数据层与目标用户现有会话撤销在同一事务完成；用户资料更新不再隐式承担密码变更语义。
- 审计查询已补齐为只读接口和页面，按当前组织隔离，支持动作、用户、时间范围和分页筛选。
- P4 固定表单 MVP 已完成：组织级主数据目录、原子批量导入契约、编号规则/并发序列、状态模板和里程碑模板版本发布均已闭环；后台页面采用固定 TypeScript/React 表单。外部文件解析和后台导入作业留在 P7，计费单位、应税服务和费用项设置调整到 P8 财务口径冻结后实现。
- P5 第一组核心聚合已完成：`Partner` 已改为组织级法人档案，通过显式角色支持客户、供应商、代理和承运人；多联系人、单一主联系人约束、别名和带原因审计的供应商黑名单已完成契约、事务仓储与固定代码页面。
- P5 第二组账户与合同已完成：客户角色结算账户支持币种、开票和银行信息、启停、默认切换及组织隔离；数据库条件唯一约束保证每个角色每种账户类型只有一个默认账户，停用账户不能设为默认。合同支持编号、日期、付款条款和明确状态机，合同编号创建后不可变，状态更新使用领域校验和仓储期望状态原子更新。账户/合同通过固定 React 抽屉页面管理，未引入低代码或动态表单。结算规则、附件、导入和导出仍待后续组实现，因此 P5 尚未宣告完成。
- P5 第三组结算规则已完成：规则按 Partner 角色隔离，支持对账模式、结算方式、结算基准、结算日、周期天数、结算币种和启停；角色/对账模式/结算方式组合唯一，按票和预付不接受周期字段，周期类规则必须明确结算基准。固定 React 页面已接入，信用额度留到 P8 金额模型。附件、导入和导出仍待后续组，因此 P5 尚未宣告完成。
- P0 门禁暂按冷启动、Party 聚合、固定表单 MVP、明确金额/汇率口径和报关/AE 后置推进；这些是当前实现假设，不代表已完成生产业务确认。

## 阶段计划

### P0：建立迁移基线

- 确认旧项目和新项目的系统边界、生产入口和最终责任归属。
- 建立旧 Prisma 模型、旧页面/API、新 Ent Schema、新 Proto 和新页面之间的迁移矩阵。
- 迁移矩阵的权威文件为 [`docs/migration-matrix.md`](docs/migration-matrix.md)，必须覆盖 12 个旧 Prisma 领域文件。
- 为每个领域标记“已完成、部分完成、未开始、明确不迁移”。
- 明确存量数据策略：冷启动，或保留旧数据并执行一次性迁移；不得把这个决策推迟到业务开发完成后。
- 将 `Partner`/`Party` 目标模型作为后续订单开发的前置门禁，先确定字段、关系、权限和业务范围。
- 记录第一阶段 MVP 范围、非功能要求、领域负责人和每阶段产出物。
- 对旧 Prisma 数据到新 Ent 数据做一次迁移可行性验证，确认 ID、枚举、组织关系、用户密码和附件是否可映射。
- 单独记录当前工作区改动，避免计划工作覆盖用户已有修改。

完成标准：形成经过评审的领域清单、依赖关系、暂不迁移清单、MVP 范围、数据保留决策、迁移可行性结论和非功能验收基线；矩阵中不得存在“未知”领域或未说明的临时兼容方案。

### P1：稳定新系统基础设施

- 固化 Kratos、Ent、Wire、PostgreSQL 和 React 管理后台的开发/构建入口。
- 完善统一错误响应、请求元数据、Trace、结构化日志和健康检查。
- 验证开发期前后端代理、生产同域静态资源托管和配置注入。
- 明确 Proto、Go 生成代码、OpenAPI 和前端客户端的生成链路。
- 固化备份恢复、数据库连接池、慢查询、文件存储、接口幂等、并发更新和生产告警要求。
- 明确登录限流、会话撤销、审计保留期限和关键接口响应时间目标。

完成标准：未登录状态下服务、数据库、健康检查、前端代理和同域静态资源均可运行；明确执行 `pnpm run check`、`pnpm run build`、`go -C server test ./...` 和 `go -C server vet ./...`。

### P2：完成认证与权限管理

- 完成用户、组织、成员关系、角色、权限、数据范围和会话管理。
- 完成登录、登出、当前用户、组织切换和会话失效。
- 增加用户管理、组织管理、角色管理和权限管理接口及页面。
- 保持后端权限校验为唯一安全边界，前端权限只用于菜单和按钮展示。

完成标准：能够完成“创建组织 → 创建用户 → 分配角色 → 登录 → 按权限访问”的完整链路，并验证组织隔离、默认拒绝规则、会话失效和权限变更生效时效。

### P3：完善审计体系

- 记录登录、登出、组织切换、用户/角色变更和业务写入操作。
- 完成审计日志查询接口和管理页面。
- 统一记录组织、用户、动作、资源、结果、请求 ID 和 Trace ID。
- 禁止将密码、Cookie、令牌和完整敏感请求/响应写入日志。

完成标准：关键安全和业务操作均可追溯，审计查询支持按组织、用户、动作和时间范围筛选。

### P4：迁移主数据与参数模板基础

- 迁移币种、国家地区、机场、港口、城市、承运人和箱型等基础资料。
- 明确每类主数据的来源、唯一键、启停规则、导入方式和审计要求。
- 迁移订单所需的服务类型、货物类别、编号规则、状态模板和里程碑模板基础能力。
- 明确模板版本、发布、恢复和审计规则，避免订单开发后再反向修改核心状态模型。
- 为订单表单提供统一的选项查询接口。

完成标准：订单表单所需基础选项、编号规则和基础模板均由新系统提供，主数据和模板查询维护不依赖旧系统。

### P5：完成往来单位模型

- 已决定保留 `Partner` 资源名并扩展为 Party 业务聚合，不保留 `customer/supplier/both` 单值类型。
- 已完成组织级法人档案、客户/供应商/代理/承运人多角色、多联系人、单一主联系人、别名和供应商黑名单。
- 已完成账户、合同和角色结算规则映射；附件、导入和导出仍待后续组。信用额度不在主数据阶段伪造金额类型，统一留到 P8。不得直接复制旧系统无默认唯一约束、无日期校验、可变合同编号和任意 JSON 扩展的设计。
- 完成列表、详情、编辑、启停、导入、导出和权限控制。
- 对旧项目字段、状态、唯一性规则和操作行为逐项对齐。
- 在模型、字段映射和业务范围评审通过前，不继续扩展依赖往来单位的订单、费用和财务接口。

完成标准：新模型与旧业务含义有明确映射；如果新系统有意缩减范围，必须记录为明确的业务决策，而不是隐式缺失；模型决策成为后续订单开发的正式门禁。

### P6：迁移订单核心

- 迁移订单基础信息、业务类型、运输方式、贸易条款、服务类型和货物信息。
- 迁移订单列表、详情、创建、编辑、查询条件和基础校验。
- 迁移订单状态、里程碑和必要的操作审计。

完成标准：订单可以完成创建、编辑、查询和基础状态流转，关键校验结果与旧系统验收场景一致。

### P7：后台任务基础与订单执行能力

- 迁移集装箱、提单、附件、协作人员、异常处理和跟踪事件。
- 迁移提醒、告警、订单操作日志和相关后台任务。
- 先建立任务幂等键、租约、超时、重试、死信、回放和任务追踪基础设施。
- 对订单状态机和跨模块副作用进行集中封装，避免业务规则落入传输层。

完成标准：订单执行链路能够独立闭环，状态变更和异常处理可追溯、可重试；后台任务失败不会静默丢失。

### P8：迁移费用、账单与汇率

- 统一设计计费单位、费用项、应税服务、默认税率及其组织边界，不把它们当作普通字典迁移。
- 迁移费用录入、费用状态、应收应付、账单生成和账单状态。
- 对齐币种、税额、汇率、费用快照、利润和账单编号口径。
- 迁移费用和账单相关的权限、审计、导出和批量操作。
- 在实现前固化金额精度、舍入、含税/不含税、应收应付方向、汇率来源、汇率快照、账单生成后修改和冲销规则。

完成标准：费用到账单的金额、币种、税额和状态流转与旧系统关键场景逐项对齐，计算口径文档和验收数据集已确认。

### P9：迁移核销、收付款、发票与佣金

- 迁移核销、收款、付款、发票、佣金和财务工作台。
- 迁移汇率快照、汇率审批和财务状态日志。
- 对金额计算、审批边界和财务审计进行专项验收。
- 明确退款、冲销、红字、佣金和利润的来源及状态机，不在页面层拼接金额规则。

完成标准：财务状态机、权限和金额来源完整可追溯，关键财务场景通过验收，且金额计算与 P8 的统一口径一致。

### P10：迁移通知、集成与运维能力

- 迁移通知、提醒规则、消息记录和用户通知偏好。
- 迁移集成任务、任务日志、重试、死信、回放和任务追踪的业务接入。
- 增加任务状态查询、失败告警、运维检查和必要的操作审计。

完成标准：通知、集成和后台作业可观测、可重试、可审计，失败不会静默丢失。

### P11：数据迁移或冷启动

根据 P0 的数据策略选择一条路径。

保留旧数据时：

- 制定旧 Prisma 数据到新 Ent 数据的字段、枚举、关系和权限映射。
- 编写一次性迁移工具，先执行多轮离线演练和只读校验。
- 校验记录数量、金额、关系、组织归属、权限和关键业务状态。
- 制定数据冻结、正式迁移、失败处理和回滚步骤。

冷启动时：

- 初始化组织、管理员、角色、权限和必要的主数据。
- 明确旧系统历史数据的只读保留、归档和查询边界。
- 验证新系统不依赖旧数据库、旧 API 或旧运行时配置。

完成标准：迁移路径或冷启动路径完成演练；保留数据时关键数据通过校验并具备回滚方案，冷启动时新系统可独立运行；不引入长期双写逻辑，除非另行批准。

### P12：正式切换与旧系统下线

- 新系统先进行完整业务验收和必要的灰度观察。
- 冻结旧系统写入，完成最终数据同步或冷启动初始化。
- 新系统成为唯一生产入口，保留必要的只读观察期。
- 关闭旧系统生产写操作，并归档迁移报告、验收记录和运维手册。

完成标准：新系统独立承载生产业务，旧系统不再承担生产写入职责。

## 当前优先级

近期按以下顺序推进，不直接跳入订单页面开发：

1. 完成新旧领域迁移矩阵。
2. 决定 `Partner` 与旧 `Party` 的目标关系。
3. 明确 MVP 范围和非功能验收基线。
4. 补齐用户、组织、角色、权限和审计管理。
5. 完成主数据、参数和模板基础服务。（固定表单 MVP 已完成）
6. 完成往来单位第一版完整模型。
7. 再开始订单核心 API 和页面。

## 验证与提交约束

- 功能完成后按风险补充针对性验证，不采用先写测试再实现的 TDD 流程。
- 服务端优先运行 `go -C server test ./...` 和 `go -C server vet ./...`。
- 前端优先运行 `pnpm --dir web lint` 和 `pnpm --dir web test`。
- 发布前必须额外运行 `pnpm run build`，并验证 Proto/OpenAPI/前端客户端生成物没有非预期差异。
- 关键业务阶段补充登录、权限、组织隔离、金额计算、状态流转和错误响应的端到端验收路径。
- P12 切换前必须完成备份恢复演练、数据迁移/冷启动演练和生产观测检查。
- 修改 Proto、配置源或 Ent Schema 后，重新生成对应代码，不手工修改生成物。
- 每完成一组可验证的改动就独立提交，使用 Conventional Commits 前缀。
- 提交前确认没有秘密、调试输出、构建缓存或本地环境文件进入版本库。
