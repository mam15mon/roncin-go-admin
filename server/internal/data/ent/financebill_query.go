// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financebill"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financebillbatch"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financebillline"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financeinvoicebill"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financeverificationallocation"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/organization"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/partner"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/predicate"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/user"
)

// FinanceBillQuery is the builder for querying FinanceBill entities.
type FinanceBillQuery struct {
	config
	ctx                         *QueryContext
	order                       []financebill.OrderOption
	inters                      []Interceptor
	predicates                  []predicate.FinanceBill
	withOrganization            *OrganizationQuery
	withBatch                   *FinanceBillBatchQuery
	withSettlementParty         *PartnerQuery
	withConfirmedByUser         *UserQuery
	withCancelledByUser         *UserQuery
	withLines                   *FinanceBillLineQuery
	withInvoiceLinks            *FinanceInvoiceBillQuery
	withVerificationAllocations *FinanceVerificationAllocationQuery
	modifiers                   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FinanceBillQuery builder.
func (_q *FinanceBillQuery) Where(ps ...predicate.FinanceBill) *FinanceBillQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *FinanceBillQuery) Limit(limit int) *FinanceBillQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *FinanceBillQuery) Offset(offset int) *FinanceBillQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *FinanceBillQuery) Unique(unique bool) *FinanceBillQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *FinanceBillQuery) Order(o ...financebill.OrderOption) *FinanceBillQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *FinanceBillQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financebill.OrganizationTable, financebill.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBatch chains the current query on the "batch" edge.
func (_q *FinanceBillQuery) QueryBatch() *FinanceBillBatchQuery {
	query := (&FinanceBillBatchClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(financebillbatch.Table, financebillbatch.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financebill.BatchTable, financebill.BatchColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySettlementParty chains the current query on the "settlement_party" edge.
func (_q *FinanceBillQuery) QuerySettlementParty() *PartnerQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financebill.SettlementPartyTable, financebill.SettlementPartyColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryConfirmedByUser chains the current query on the "confirmed_by_user" edge.
func (_q *FinanceBillQuery) QueryConfirmedByUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financebill.ConfirmedByUserTable, financebill.ConfirmedByUserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCancelledByUser chains the current query on the "cancelled_by_user" edge.
func (_q *FinanceBillQuery) QueryCancelledByUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financebill.CancelledByUserTable, financebill.CancelledByUserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLines chains the current query on the "lines" edge.
func (_q *FinanceBillQuery) QueryLines() *FinanceBillLineQuery {
	query := (&FinanceBillLineClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(financebillline.Table, financebillline.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, financebill.LinesTable, financebill.LinesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInvoiceLinks chains the current query on the "invoice_links" edge.
func (_q *FinanceBillQuery) QueryInvoiceLinks() *FinanceInvoiceBillQuery {
	query := (&FinanceInvoiceBillClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(financeinvoicebill.Table, financeinvoicebill.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, financebill.InvoiceLinksTable, financebill.InvoiceLinksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVerificationAllocations chains the current query on the "verification_allocations" edge.
func (_q *FinanceBillQuery) QueryVerificationAllocations() *FinanceVerificationAllocationQuery {
	query := (&FinanceVerificationAllocationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financebill.Table, financebill.FieldID, selector),
			sqlgraph.To(financeverificationallocation.Table, financeverificationallocation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, financebill.VerificationAllocationsTable, financebill.VerificationAllocationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FinanceBill entity from the query.
// Returns a *NotFoundError when no FinanceBill was found.
func (_q *FinanceBillQuery) First(ctx context.Context) (*FinanceBill, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{financebill.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *FinanceBillQuery) FirstX(ctx context.Context) *FinanceBill {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FinanceBill ID from the query.
// Returns a *NotFoundError when no FinanceBill ID was found.
func (_q *FinanceBillQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{financebill.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *FinanceBillQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FinanceBill entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FinanceBill entity is found.
// Returns a *NotFoundError when no FinanceBill entities are found.
func (_q *FinanceBillQuery) Only(ctx context.Context) (*FinanceBill, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{financebill.Label}
	default:
		return nil, &NotSingularError{financebill.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *FinanceBillQuery) OnlyX(ctx context.Context) *FinanceBill {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FinanceBill ID in the query.
// Returns a *NotSingularError when more than one FinanceBill ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *FinanceBillQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{financebill.Label}
	default:
		err = &NotSingularError{financebill.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *FinanceBillQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FinanceBills.
func (_q *FinanceBillQuery) All(ctx context.Context) ([]*FinanceBill, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FinanceBill, *FinanceBillQuery]()
	return withInterceptors[[]*FinanceBill](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *FinanceBillQuery) AllX(ctx context.Context) []*FinanceBill {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FinanceBill IDs.
func (_q *FinanceBillQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(financebill.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *FinanceBillQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *FinanceBillQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*FinanceBillQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *FinanceBillQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *FinanceBillQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *FinanceBillQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FinanceBillQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *FinanceBillQuery) Clone() *FinanceBillQuery {
	if _q == nil {
		return nil
	}
	return &FinanceBillQuery{
		config:                      _q.config,
		ctx:                         _q.ctx.Clone(),
		order:                       append([]financebill.OrderOption{}, _q.order...),
		inters:                      append([]Interceptor{}, _q.inters...),
		predicates:                  append([]predicate.FinanceBill{}, _q.predicates...),
		withOrganization:            _q.withOrganization.Clone(),
		withBatch:                   _q.withBatch.Clone(),
		withSettlementParty:         _q.withSettlementParty.Clone(),
		withConfirmedByUser:         _q.withConfirmedByUser.Clone(),
		withCancelledByUser:         _q.withCancelledByUser.Clone(),
		withLines:                   _q.withLines.Clone(),
		withInvoiceLinks:            _q.withInvoiceLinks.Clone(),
		withVerificationAllocations: _q.withVerificationAllocations.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithOrganization(opts ...func(*OrganizationQuery)) *FinanceBillQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithBatch tells the query-builder to eager-load the nodes that are connected to
// the "batch" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithBatch(opts ...func(*FinanceBillBatchQuery)) *FinanceBillQuery {
	query := (&FinanceBillBatchClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBatch = query
	return _q
}

// WithSettlementParty tells the query-builder to eager-load the nodes that are connected to
// the "settlement_party" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithSettlementParty(opts ...func(*PartnerQuery)) *FinanceBillQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSettlementParty = query
	return _q
}

// WithConfirmedByUser tells the query-builder to eager-load the nodes that are connected to
// the "confirmed_by_user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithConfirmedByUser(opts ...func(*UserQuery)) *FinanceBillQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withConfirmedByUser = query
	return _q
}

// WithCancelledByUser tells the query-builder to eager-load the nodes that are connected to
// the "cancelled_by_user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithCancelledByUser(opts ...func(*UserQuery)) *FinanceBillQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCancelledByUser = query
	return _q
}

// WithLines tells the query-builder to eager-load the nodes that are connected to
// the "lines" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithLines(opts ...func(*FinanceBillLineQuery)) *FinanceBillQuery {
	query := (&FinanceBillLineClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLines = query
	return _q
}

// WithInvoiceLinks tells the query-builder to eager-load the nodes that are connected to
// the "invoice_links" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithInvoiceLinks(opts ...func(*FinanceInvoiceBillQuery)) *FinanceBillQuery {
	query := (&FinanceInvoiceBillClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInvoiceLinks = query
	return _q
}

// WithVerificationAllocations tells the query-builder to eager-load the nodes that are connected to
// the "verification_allocations" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FinanceBillQuery) WithVerificationAllocations(opts ...func(*FinanceVerificationAllocationQuery)) *FinanceBillQuery {
	query := (&FinanceVerificationAllocationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVerificationAllocations = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FinanceBill.Query().
//		GroupBy(financebill.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *FinanceBillQuery) GroupBy(field string, fields ...string) *FinanceBillGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FinanceBillGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = financebill.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.FinanceBill.Query().
//		Select(financebill.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *FinanceBillQuery) Select(fields ...string) *FinanceBillSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &FinanceBillSelect{FinanceBillQuery: _q}
	sbuild.label = financebill.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FinanceBillSelect configured with the given aggregations.
func (_q *FinanceBillQuery) Aggregate(fns ...AggregateFunc) *FinanceBillSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *FinanceBillQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !financebill.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *FinanceBillQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FinanceBill, error) {
	var (
		nodes       = []*FinanceBill{}
		_spec       = _q.querySpec()
		loadedTypes = [8]bool{
			_q.withOrganization != nil,
			_q.withBatch != nil,
			_q.withSettlementParty != nil,
			_q.withConfirmedByUser != nil,
			_q.withCancelledByUser != nil,
			_q.withLines != nil,
			_q.withInvoiceLinks != nil,
			_q.withVerificationAllocations != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FinanceBill).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FinanceBill{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *FinanceBill, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBatch; query != nil {
		if err := _q.loadBatch(ctx, query, nodes, nil,
			func(n *FinanceBill, e *FinanceBillBatch) { n.Edges.Batch = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSettlementParty; query != nil {
		if err := _q.loadSettlementParty(ctx, query, nodes, nil,
			func(n *FinanceBill, e *Partner) { n.Edges.SettlementParty = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withConfirmedByUser; query != nil {
		if err := _q.loadConfirmedByUser(ctx, query, nodes, nil,
			func(n *FinanceBill, e *User) { n.Edges.ConfirmedByUser = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCancelledByUser; query != nil {
		if err := _q.loadCancelledByUser(ctx, query, nodes, nil,
			func(n *FinanceBill, e *User) { n.Edges.CancelledByUser = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLines; query != nil {
		if err := _q.loadLines(ctx, query, nodes,
			func(n *FinanceBill) { n.Edges.Lines = []*FinanceBillLine{} },
			func(n *FinanceBill, e *FinanceBillLine) { n.Edges.Lines = append(n.Edges.Lines, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInvoiceLinks; query != nil {
		if err := _q.loadInvoiceLinks(ctx, query, nodes,
			func(n *FinanceBill) { n.Edges.InvoiceLinks = []*FinanceInvoiceBill{} },
			func(n *FinanceBill, e *FinanceInvoiceBill) { n.Edges.InvoiceLinks = append(n.Edges.InvoiceLinks, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVerificationAllocations; query != nil {
		if err := _q.loadVerificationAllocations(ctx, query, nodes,
			func(n *FinanceBill) { n.Edges.VerificationAllocations = []*FinanceVerificationAllocation{} },
			func(n *FinanceBill, e *FinanceVerificationAllocation) {
				n.Edges.VerificationAllocations = append(n.Edges.VerificationAllocations, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *FinanceBillQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinanceBill)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FinanceBillQuery) loadBatch(ctx context.Context, query *FinanceBillBatchQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *FinanceBillBatch)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinanceBill)
	for i := range nodes {
		if nodes[i].BatchID == nil {
			continue
		}
		fk := *nodes[i].BatchID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(financebillbatch.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "batch_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FinanceBillQuery) loadSettlementParty(ctx context.Context, query *PartnerQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *Partner)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinanceBill)
	for i := range nodes {
		fk := nodes[i].SettlementPartyID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "settlement_party_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FinanceBillQuery) loadConfirmedByUser(ctx context.Context, query *UserQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinanceBill)
	for i := range nodes {
		if nodes[i].ConfirmedBy == nil {
			continue
		}
		fk := *nodes[i].ConfirmedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "confirmed_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FinanceBillQuery) loadCancelledByUser(ctx context.Context, query *UserQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinanceBill)
	for i := range nodes {
		if nodes[i].CancelledBy == nil {
			continue
		}
		fk := *nodes[i].CancelledBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "cancelled_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FinanceBillQuery) loadLines(ctx context.Context, query *FinanceBillLineQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *FinanceBillLine)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*FinanceBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(financebillline.FieldBillID)
	}
	query.Where(predicate.FinanceBillLine(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(financebill.LinesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *FinanceBillQuery) loadInvoiceLinks(ctx context.Context, query *FinanceInvoiceBillQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *FinanceInvoiceBill)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*FinanceBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(financeinvoicebill.FieldBillID)
	}
	query.Where(predicate.FinanceInvoiceBill(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(financebill.InvoiceLinksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *FinanceBillQuery) loadVerificationAllocations(ctx context.Context, query *FinanceVerificationAllocationQuery, nodes []*FinanceBill, init func(*FinanceBill), assign func(*FinanceBill, *FinanceVerificationAllocation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*FinanceBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(financeverificationallocation.FieldBillID)
	}
	query.Where(predicate.FinanceVerificationAllocation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(financebill.VerificationAllocationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *FinanceBillQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *FinanceBillQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(financebill.Table, financebill.Columns, sqlgraph.NewFieldSpec(financebill.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, financebill.FieldID)
		for i := range fields {
			if fields[i] != financebill.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(financebill.FieldOrganizationID)
		}
		if _q.withBatch != nil {
			_spec.Node.AddColumnOnce(financebill.FieldBatchID)
		}
		if _q.withSettlementParty != nil {
			_spec.Node.AddColumnOnce(financebill.FieldSettlementPartyID)
		}
		if _q.withConfirmedByUser != nil {
			_spec.Node.AddColumnOnce(financebill.FieldConfirmedBy)
		}
		if _q.withCancelledByUser != nil {
			_spec.Node.AddColumnOnce(financebill.FieldCancelledBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *FinanceBillQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(financebill.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = financebill.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *FinanceBillQuery) ForUpdate(opts ...sql.LockOption) *FinanceBillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *FinanceBillQuery) ForShare(opts ...sql.LockOption) *FinanceBillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// FinanceBillGroupBy is the group-by builder for FinanceBill entities.
type FinanceBillGroupBy struct {
	selector
	build *FinanceBillQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *FinanceBillGroupBy) Aggregate(fns ...AggregateFunc) *FinanceBillGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *FinanceBillGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FinanceBillQuery, *FinanceBillGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *FinanceBillGroupBy) sqlScan(ctx context.Context, root *FinanceBillQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FinanceBillSelect is the builder for selecting fields of FinanceBill entities.
type FinanceBillSelect struct {
	*FinanceBillQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *FinanceBillSelect) Aggregate(fns ...AggregateFunc) *FinanceBillSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *FinanceBillSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FinanceBillQuery, *FinanceBillSelect](ctx, _s.FinanceBillQuery, _s, _s.inters, v)
}

func (_s *FinanceBillSelect) sqlScan(ctx context.Context, root *FinanceBillQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
