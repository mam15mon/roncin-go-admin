// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/order"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/orderlockhousebillsnapshot"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/organization"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/partner"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/predicate"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seacargoallocation"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seadocumentvoidevent"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seahousebill"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seahousebillswitchevent"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seahousebillversion"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/seamasterbill"
)

// SeaHouseBillQuery is the builder for querying SeaHouseBill entities.
type SeaHouseBillQuery struct {
	config
	ctx                    *QueryContext
	order                  []seahousebill.OrderOption
	inters                 []Interceptor
	predicates             []predicate.SeaHouseBill
	withOrganization       *OrganizationQuery
	withOrder              *OrderQuery
	withMasterBill         *SeaMasterBillQuery
	withIssuerOrganization *OrganizationQuery
	withIssuerPartner      *PartnerQuery
	withCargoAllocations   *SeaCargoAllocationQuery
	withCurrentVersion     *SeaHouseBillVersionQuery
	withVersions           *SeaHouseBillVersionQuery
	withLockSnapshots      *OrderLockHouseBillSnapshotQuery
	withVoidEvents         *SeaDocumentVoidEventQuery
	withOldSwitchEvents    *SeaHouseBillSwitchEventQuery
	withNewSwitchEvents    *SeaHouseBillSwitchEventQuery
	modifiers              []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SeaHouseBillQuery builder.
func (_q *SeaHouseBillQuery) Where(ps ...predicate.SeaHouseBill) *SeaHouseBillQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SeaHouseBillQuery) Limit(limit int) *SeaHouseBillQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SeaHouseBillQuery) Offset(offset int) *SeaHouseBillQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SeaHouseBillQuery) Unique(unique bool) *SeaHouseBillQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SeaHouseBillQuery) Order(o ...seahousebill.OrderOption) *SeaHouseBillQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *SeaHouseBillQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seahousebill.OrganizationTable, seahousebill.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (_q *SeaHouseBillQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seahousebill.OrderTable, seahousebill.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMasterBill chains the current query on the "master_bill" edge.
func (_q *SeaHouseBillQuery) QueryMasterBill() *SeaMasterBillQuery {
	query := (&SeaMasterBillClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seamasterbill.Table, seamasterbill.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seahousebill.MasterBillTable, seahousebill.MasterBillColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIssuerOrganization chains the current query on the "issuer_organization" edge.
func (_q *SeaHouseBillQuery) QueryIssuerOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seahousebill.IssuerOrganizationTable, seahousebill.IssuerOrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIssuerPartner chains the current query on the "issuer_partner" edge.
func (_q *SeaHouseBillQuery) QueryIssuerPartner() *PartnerQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seahousebill.IssuerPartnerTable, seahousebill.IssuerPartnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCargoAllocations chains the current query on the "cargo_allocations" edge.
func (_q *SeaHouseBillQuery) QueryCargoAllocations() *SeaCargoAllocationQuery {
	query := (&SeaCargoAllocationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seacargoallocation.Table, seacargoallocation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.CargoAllocationsTable, seahousebill.CargoAllocationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCurrentVersion chains the current query on the "current_version" edge.
func (_q *SeaHouseBillQuery) QueryCurrentVersion() *SeaHouseBillVersionQuery {
	query := (&SeaHouseBillVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seahousebillversion.Table, seahousebillversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, seahousebill.CurrentVersionTable, seahousebill.CurrentVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVersions chains the current query on the "versions" edge.
func (_q *SeaHouseBillQuery) QueryVersions() *SeaHouseBillVersionQuery {
	query := (&SeaHouseBillVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seahousebillversion.Table, seahousebillversion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.VersionsTable, seahousebill.VersionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLockSnapshots chains the current query on the "lock_snapshots" edge.
func (_q *SeaHouseBillQuery) QueryLockSnapshots() *OrderLockHouseBillSnapshotQuery {
	query := (&OrderLockHouseBillSnapshotClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(orderlockhousebillsnapshot.Table, orderlockhousebillsnapshot.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.LockSnapshotsTable, seahousebill.LockSnapshotsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVoidEvents chains the current query on the "void_events" edge.
func (_q *SeaHouseBillQuery) QueryVoidEvents() *SeaDocumentVoidEventQuery {
	query := (&SeaDocumentVoidEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seadocumentvoidevent.Table, seadocumentvoidevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.VoidEventsTable, seahousebill.VoidEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOldSwitchEvents chains the current query on the "old_switch_events" edge.
func (_q *SeaHouseBillQuery) QueryOldSwitchEvents() *SeaHouseBillSwitchEventQuery {
	query := (&SeaHouseBillSwitchEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seahousebillswitchevent.Table, seahousebillswitchevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.OldSwitchEventsTable, seahousebill.OldSwitchEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNewSwitchEvents chains the current query on the "new_switch_events" edge.
func (_q *SeaHouseBillQuery) QueryNewSwitchEvents() *SeaHouseBillSwitchEventQuery {
	query := (&SeaHouseBillSwitchEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seahousebill.Table, seahousebill.FieldID, selector),
			sqlgraph.To(seahousebillswitchevent.Table, seahousebillswitchevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, seahousebill.NewSwitchEventsTable, seahousebill.NewSwitchEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SeaHouseBill entity from the query.
// Returns a *NotFoundError when no SeaHouseBill was found.
func (_q *SeaHouseBillQuery) First(ctx context.Context) (*SeaHouseBill, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{seahousebill.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SeaHouseBillQuery) FirstX(ctx context.Context) *SeaHouseBill {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SeaHouseBill ID from the query.
// Returns a *NotFoundError when no SeaHouseBill ID was found.
func (_q *SeaHouseBillQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{seahousebill.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SeaHouseBillQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SeaHouseBill entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SeaHouseBill entity is found.
// Returns a *NotFoundError when no SeaHouseBill entities are found.
func (_q *SeaHouseBillQuery) Only(ctx context.Context) (*SeaHouseBill, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{seahousebill.Label}
	default:
		return nil, &NotSingularError{seahousebill.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SeaHouseBillQuery) OnlyX(ctx context.Context) *SeaHouseBill {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SeaHouseBill ID in the query.
// Returns a *NotSingularError when more than one SeaHouseBill ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SeaHouseBillQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{seahousebill.Label}
	default:
		err = &NotSingularError{seahousebill.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SeaHouseBillQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SeaHouseBills.
func (_q *SeaHouseBillQuery) All(ctx context.Context) ([]*SeaHouseBill, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SeaHouseBill, *SeaHouseBillQuery]()
	return withInterceptors[[]*SeaHouseBill](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SeaHouseBillQuery) AllX(ctx context.Context) []*SeaHouseBill {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SeaHouseBill IDs.
func (_q *SeaHouseBillQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(seahousebill.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SeaHouseBillQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SeaHouseBillQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SeaHouseBillQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SeaHouseBillQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SeaHouseBillQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SeaHouseBillQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SeaHouseBillQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SeaHouseBillQuery) Clone() *SeaHouseBillQuery {
	if _q == nil {
		return nil
	}
	return &SeaHouseBillQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]seahousebill.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.SeaHouseBill{}, _q.predicates...),
		withOrganization:       _q.withOrganization.Clone(),
		withOrder:              _q.withOrder.Clone(),
		withMasterBill:         _q.withMasterBill.Clone(),
		withIssuerOrganization: _q.withIssuerOrganization.Clone(),
		withIssuerPartner:      _q.withIssuerPartner.Clone(),
		withCargoAllocations:   _q.withCargoAllocations.Clone(),
		withCurrentVersion:     _q.withCurrentVersion.Clone(),
		withVersions:           _q.withVersions.Clone(),
		withLockSnapshots:      _q.withLockSnapshots.Clone(),
		withVoidEvents:         _q.withVoidEvents.Clone(),
		withOldSwitchEvents:    _q.withOldSwitchEvents.Clone(),
		withNewSwitchEvents:    _q.withNewSwitchEvents.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithOrganization(opts ...func(*OrganizationQuery)) *SeaHouseBillQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithOrder(opts ...func(*OrderQuery)) *SeaHouseBillQuery {
	query := (&OrderClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrder = query
	return _q
}

// WithMasterBill tells the query-builder to eager-load the nodes that are connected to
// the "master_bill" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithMasterBill(opts ...func(*SeaMasterBillQuery)) *SeaHouseBillQuery {
	query := (&SeaMasterBillClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMasterBill = query
	return _q
}

// WithIssuerOrganization tells the query-builder to eager-load the nodes that are connected to
// the "issuer_organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithIssuerOrganization(opts ...func(*OrganizationQuery)) *SeaHouseBillQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIssuerOrganization = query
	return _q
}

// WithIssuerPartner tells the query-builder to eager-load the nodes that are connected to
// the "issuer_partner" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithIssuerPartner(opts ...func(*PartnerQuery)) *SeaHouseBillQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIssuerPartner = query
	return _q
}

// WithCargoAllocations tells the query-builder to eager-load the nodes that are connected to
// the "cargo_allocations" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithCargoAllocations(opts ...func(*SeaCargoAllocationQuery)) *SeaHouseBillQuery {
	query := (&SeaCargoAllocationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCargoAllocations = query
	return _q
}

// WithCurrentVersion tells the query-builder to eager-load the nodes that are connected to
// the "current_version" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithCurrentVersion(opts ...func(*SeaHouseBillVersionQuery)) *SeaHouseBillQuery {
	query := (&SeaHouseBillVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCurrentVersion = query
	return _q
}

// WithVersions tells the query-builder to eager-load the nodes that are connected to
// the "versions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithVersions(opts ...func(*SeaHouseBillVersionQuery)) *SeaHouseBillQuery {
	query := (&SeaHouseBillVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVersions = query
	return _q
}

// WithLockSnapshots tells the query-builder to eager-load the nodes that are connected to
// the "lock_snapshots" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithLockSnapshots(opts ...func(*OrderLockHouseBillSnapshotQuery)) *SeaHouseBillQuery {
	query := (&OrderLockHouseBillSnapshotClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLockSnapshots = query
	return _q
}

// WithVoidEvents tells the query-builder to eager-load the nodes that are connected to
// the "void_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithVoidEvents(opts ...func(*SeaDocumentVoidEventQuery)) *SeaHouseBillQuery {
	query := (&SeaDocumentVoidEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVoidEvents = query
	return _q
}

// WithOldSwitchEvents tells the query-builder to eager-load the nodes that are connected to
// the "old_switch_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithOldSwitchEvents(opts ...func(*SeaHouseBillSwitchEventQuery)) *SeaHouseBillQuery {
	query := (&SeaHouseBillSwitchEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOldSwitchEvents = query
	return _q
}

// WithNewSwitchEvents tells the query-builder to eager-load the nodes that are connected to
// the "new_switch_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeaHouseBillQuery) WithNewSwitchEvents(opts ...func(*SeaHouseBillSwitchEventQuery)) *SeaHouseBillQuery {
	query := (&SeaHouseBillSwitchEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNewSwitchEvents = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SeaHouseBill.Query().
//		GroupBy(seahousebill.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SeaHouseBillQuery) GroupBy(field string, fields ...string) *SeaHouseBillGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SeaHouseBillGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = seahousebill.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SeaHouseBill.Query().
//		Select(seahousebill.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *SeaHouseBillQuery) Select(fields ...string) *SeaHouseBillSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SeaHouseBillSelect{SeaHouseBillQuery: _q}
	sbuild.label = seahousebill.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SeaHouseBillSelect configured with the given aggregations.
func (_q *SeaHouseBillQuery) Aggregate(fns ...AggregateFunc) *SeaHouseBillSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SeaHouseBillQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !seahousebill.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SeaHouseBillQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SeaHouseBill, error) {
	var (
		nodes       = []*SeaHouseBill{}
		_spec       = _q.querySpec()
		loadedTypes = [12]bool{
			_q.withOrganization != nil,
			_q.withOrder != nil,
			_q.withMasterBill != nil,
			_q.withIssuerOrganization != nil,
			_q.withIssuerPartner != nil,
			_q.withCargoAllocations != nil,
			_q.withCurrentVersion != nil,
			_q.withVersions != nil,
			_q.withLockSnapshots != nil,
			_q.withVoidEvents != nil,
			_q.withOldSwitchEvents != nil,
			_q.withNewSwitchEvents != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SeaHouseBill).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SeaHouseBill{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOrder; query != nil {
		if err := _q.loadOrder(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMasterBill; query != nil {
		if err := _q.loadMasterBill(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *SeaMasterBill) { n.Edges.MasterBill = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withIssuerOrganization; query != nil {
		if err := _q.loadIssuerOrganization(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *Organization) { n.Edges.IssuerOrganization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withIssuerPartner; query != nil {
		if err := _q.loadIssuerPartner(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *Partner) { n.Edges.IssuerPartner = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCargoAllocations; query != nil {
		if err := _q.loadCargoAllocations(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.CargoAllocations = []*SeaCargoAllocation{} },
			func(n *SeaHouseBill, e *SeaCargoAllocation) {
				n.Edges.CargoAllocations = append(n.Edges.CargoAllocations, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withCurrentVersion; query != nil {
		if err := _q.loadCurrentVersion(ctx, query, nodes, nil,
			func(n *SeaHouseBill, e *SeaHouseBillVersion) { n.Edges.CurrentVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVersions; query != nil {
		if err := _q.loadVersions(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.Versions = []*SeaHouseBillVersion{} },
			func(n *SeaHouseBill, e *SeaHouseBillVersion) { n.Edges.Versions = append(n.Edges.Versions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLockSnapshots; query != nil {
		if err := _q.loadLockSnapshots(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.LockSnapshots = []*OrderLockHouseBillSnapshot{} },
			func(n *SeaHouseBill, e *OrderLockHouseBillSnapshot) {
				n.Edges.LockSnapshots = append(n.Edges.LockSnapshots, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withVoidEvents; query != nil {
		if err := _q.loadVoidEvents(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.VoidEvents = []*SeaDocumentVoidEvent{} },
			func(n *SeaHouseBill, e *SeaDocumentVoidEvent) { n.Edges.VoidEvents = append(n.Edges.VoidEvents, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOldSwitchEvents; query != nil {
		if err := _q.loadOldSwitchEvents(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.OldSwitchEvents = []*SeaHouseBillSwitchEvent{} },
			func(n *SeaHouseBill, e *SeaHouseBillSwitchEvent) {
				n.Edges.OldSwitchEvents = append(n.Edges.OldSwitchEvents, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withNewSwitchEvents; query != nil {
		if err := _q.loadNewSwitchEvents(ctx, query, nodes,
			func(n *SeaHouseBill) { n.Edges.NewSwitchEvents = []*SeaHouseBillSwitchEvent{} },
			func(n *SeaHouseBill, e *SeaHouseBillSwitchEvent) {
				n.Edges.NewSwitchEvents = append(n.Edges.NewSwitchEvents, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SeaHouseBillQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *Order)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		fk := nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadMasterBill(ctx context.Context, query *SeaMasterBillQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaMasterBill)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		fk := nodes[i].MasterBillID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(seamasterbill.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "master_bill_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadIssuerOrganization(ctx context.Context, query *OrganizationQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		if nodes[i].IssuerOrganizationID == nil {
			continue
		}
		fk := *nodes[i].IssuerOrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "issuer_organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadIssuerPartner(ctx context.Context, query *PartnerQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *Partner)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		if nodes[i].IssuerPartnerID == nil {
			continue
		}
		fk := *nodes[i].IssuerPartnerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "issuer_partner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadCargoAllocations(ctx context.Context, query *SeaCargoAllocationQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaCargoAllocation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(seacargoallocation.FieldHouseBillID)
	}
	query.Where(predicate.SeaCargoAllocation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.CargoAllocationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HouseBillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "house_bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadCurrentVersion(ctx context.Context, query *SeaHouseBillVersionQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaHouseBillVersion)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeaHouseBill)
	for i := range nodes {
		if nodes[i].CurrentVersionID == nil {
			continue
		}
		fk := *nodes[i].CurrentVersionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(seahousebillversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "current_version_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadVersions(ctx context.Context, query *SeaHouseBillVersionQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaHouseBillVersion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(seahousebillversion.FieldHouseBillID)
	}
	query.Where(predicate.SeaHouseBillVersion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.VersionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HouseBillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "house_bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadLockSnapshots(ctx context.Context, query *OrderLockHouseBillSnapshotQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *OrderLockHouseBillSnapshot)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(orderlockhousebillsnapshot.FieldHouseBillID)
	}
	query.Where(predicate.OrderLockHouseBillSnapshot(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.LockSnapshotsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HouseBillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "house_bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadVoidEvents(ctx context.Context, query *SeaDocumentVoidEventQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaDocumentVoidEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(seadocumentvoidevent.FieldHouseBillID)
	}
	query.Where(predicate.SeaDocumentVoidEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.VoidEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HouseBillID
		if fk == nil {
			return fmt.Errorf(`foreign-key "house_bill_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "house_bill_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadOldSwitchEvents(ctx context.Context, query *SeaHouseBillSwitchEventQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaHouseBillSwitchEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(seahousebillswitchevent.FieldOldHouseBillID)
	}
	query.Where(predicate.SeaHouseBillSwitchEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.OldSwitchEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.OldHouseBillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "old_house_bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SeaHouseBillQuery) loadNewSwitchEvents(ctx context.Context, query *SeaHouseBillSwitchEventQuery, nodes []*SeaHouseBill, init func(*SeaHouseBill), assign func(*SeaHouseBill, *SeaHouseBillSwitchEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SeaHouseBill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(seahousebillswitchevent.FieldNewHouseBillID)
	}
	query.Where(predicate.SeaHouseBillSwitchEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(seahousebill.NewSwitchEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.NewHouseBillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "new_house_bill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SeaHouseBillQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SeaHouseBillQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(seahousebill.Table, seahousebill.Columns, sqlgraph.NewFieldSpec(seahousebill.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, seahousebill.FieldID)
		for i := range fields {
			if fields[i] != seahousebill.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldOrganizationID)
		}
		if _q.withOrder != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldOrderID)
		}
		if _q.withMasterBill != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldMasterBillID)
		}
		if _q.withIssuerOrganization != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldIssuerOrganizationID)
		}
		if _q.withIssuerPartner != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldIssuerPartnerID)
		}
		if _q.withCurrentVersion != nil {
			_spec.Node.AddColumnOnce(seahousebill.FieldCurrentVersionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SeaHouseBillQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(seahousebill.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = seahousebill.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *SeaHouseBillQuery) ForUpdate(opts ...sql.LockOption) *SeaHouseBillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *SeaHouseBillQuery) ForShare(opts ...sql.LockOption) *SeaHouseBillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// SeaHouseBillGroupBy is the group-by builder for SeaHouseBill entities.
type SeaHouseBillGroupBy struct {
	selector
	build *SeaHouseBillQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SeaHouseBillGroupBy) Aggregate(fns ...AggregateFunc) *SeaHouseBillGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SeaHouseBillGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeaHouseBillQuery, *SeaHouseBillGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SeaHouseBillGroupBy) sqlScan(ctx context.Context, root *SeaHouseBillQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SeaHouseBillSelect is the builder for selecting fields of SeaHouseBill entities.
type SeaHouseBillSelect struct {
	*SeaHouseBillQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SeaHouseBillSelect) Aggregate(fns ...AggregateFunc) *SeaHouseBillSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SeaHouseBillSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeaHouseBillQuery, *SeaHouseBillSelect](ctx, _s.SeaHouseBillQuery, _s, _s.inters, v)
}

func (_s *SeaHouseBillSelect) sqlScan(ctx context.Context, root *SeaHouseBillQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
