// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/backgroundtask"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/dingtalkapprovaldispatch"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/orderunlockrequest"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/organization"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/predicate"
)

// DingTalkApprovalDispatchQuery is the builder for querying DingTalkApprovalDispatch entities.
type DingTalkApprovalDispatchQuery struct {
	config
	ctx                *QueryContext
	order              []dingtalkapprovaldispatch.OrderOption
	inters             []Interceptor
	predicates         []predicate.DingTalkApprovalDispatch
	withOrganization   *OrganizationQuery
	withBackgroundTask *BackgroundTaskQuery
	withUnlockRequest  *OrderUnlockRequestQuery
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DingTalkApprovalDispatchQuery builder.
func (_q *DingTalkApprovalDispatchQuery) Where(ps ...predicate.DingTalkApprovalDispatch) *DingTalkApprovalDispatchQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *DingTalkApprovalDispatchQuery) Limit(limit int) *DingTalkApprovalDispatchQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *DingTalkApprovalDispatchQuery) Offset(offset int) *DingTalkApprovalDispatchQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *DingTalkApprovalDispatchQuery) Unique(unique bool) *DingTalkApprovalDispatchQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *DingTalkApprovalDispatchQuery) Order(o ...dingtalkapprovaldispatch.OrderOption) *DingTalkApprovalDispatchQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *DingTalkApprovalDispatchQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dingtalkapprovaldispatch.Table, dingtalkapprovaldispatch.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, dingtalkapprovaldispatch.OrganizationTable, dingtalkapprovaldispatch.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBackgroundTask chains the current query on the "background_task" edge.
func (_q *DingTalkApprovalDispatchQuery) QueryBackgroundTask() *BackgroundTaskQuery {
	query := (&BackgroundTaskClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dingtalkapprovaldispatch.Table, dingtalkapprovaldispatch.FieldID, selector),
			sqlgraph.To(backgroundtask.Table, backgroundtask.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, dingtalkapprovaldispatch.BackgroundTaskTable, dingtalkapprovaldispatch.BackgroundTaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUnlockRequest chains the current query on the "unlock_request" edge.
func (_q *DingTalkApprovalDispatchQuery) QueryUnlockRequest() *OrderUnlockRequestQuery {
	query := (&OrderUnlockRequestClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dingtalkapprovaldispatch.Table, dingtalkapprovaldispatch.FieldID, selector),
			sqlgraph.To(orderunlockrequest.Table, orderunlockrequest.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, dingtalkapprovaldispatch.UnlockRequestTable, dingtalkapprovaldispatch.UnlockRequestColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DingTalkApprovalDispatch entity from the query.
// Returns a *NotFoundError when no DingTalkApprovalDispatch was found.
func (_q *DingTalkApprovalDispatchQuery) First(ctx context.Context) (*DingTalkApprovalDispatch, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dingtalkapprovaldispatch.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) FirstX(ctx context.Context) *DingTalkApprovalDispatch {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DingTalkApprovalDispatch ID from the query.
// Returns a *NotFoundError when no DingTalkApprovalDispatch ID was found.
func (_q *DingTalkApprovalDispatchQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dingtalkapprovaldispatch.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DingTalkApprovalDispatch entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DingTalkApprovalDispatch entity is found.
// Returns a *NotFoundError when no DingTalkApprovalDispatch entities are found.
func (_q *DingTalkApprovalDispatchQuery) Only(ctx context.Context) (*DingTalkApprovalDispatch, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dingtalkapprovaldispatch.Label}
	default:
		return nil, &NotSingularError{dingtalkapprovaldispatch.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) OnlyX(ctx context.Context) *DingTalkApprovalDispatch {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DingTalkApprovalDispatch ID in the query.
// Returns a *NotSingularError when more than one DingTalkApprovalDispatch ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *DingTalkApprovalDispatchQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dingtalkapprovaldispatch.Label}
	default:
		err = &NotSingularError{dingtalkapprovaldispatch.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DingTalkApprovalDispatches.
func (_q *DingTalkApprovalDispatchQuery) All(ctx context.Context) ([]*DingTalkApprovalDispatch, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DingTalkApprovalDispatch, *DingTalkApprovalDispatchQuery]()
	return withInterceptors[[]*DingTalkApprovalDispatch](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) AllX(ctx context.Context) []*DingTalkApprovalDispatch {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DingTalkApprovalDispatch IDs.
func (_q *DingTalkApprovalDispatchQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(dingtalkapprovaldispatch.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *DingTalkApprovalDispatchQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*DingTalkApprovalDispatchQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *DingTalkApprovalDispatchQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *DingTalkApprovalDispatchQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DingTalkApprovalDispatchQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *DingTalkApprovalDispatchQuery) Clone() *DingTalkApprovalDispatchQuery {
	if _q == nil {
		return nil
	}
	return &DingTalkApprovalDispatchQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]dingtalkapprovaldispatch.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.DingTalkApprovalDispatch{}, _q.predicates...),
		withOrganization:   _q.withOrganization.Clone(),
		withBackgroundTask: _q.withBackgroundTask.Clone(),
		withUnlockRequest:  _q.withUnlockRequest.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DingTalkApprovalDispatchQuery) WithOrganization(opts ...func(*OrganizationQuery)) *DingTalkApprovalDispatchQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithBackgroundTask tells the query-builder to eager-load the nodes that are connected to
// the "background_task" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DingTalkApprovalDispatchQuery) WithBackgroundTask(opts ...func(*BackgroundTaskQuery)) *DingTalkApprovalDispatchQuery {
	query := (&BackgroundTaskClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBackgroundTask = query
	return _q
}

// WithUnlockRequest tells the query-builder to eager-load the nodes that are connected to
// the "unlock_request" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DingTalkApprovalDispatchQuery) WithUnlockRequest(opts ...func(*OrderUnlockRequestQuery)) *DingTalkApprovalDispatchQuery {
	query := (&OrderUnlockRequestClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUnlockRequest = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DingTalkApprovalDispatch.Query().
//		GroupBy(dingtalkapprovaldispatch.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *DingTalkApprovalDispatchQuery) GroupBy(field string, fields ...string) *DingTalkApprovalDispatchGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DingTalkApprovalDispatchGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = dingtalkapprovaldispatch.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.DingTalkApprovalDispatch.Query().
//		Select(dingtalkapprovaldispatch.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *DingTalkApprovalDispatchQuery) Select(fields ...string) *DingTalkApprovalDispatchSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &DingTalkApprovalDispatchSelect{DingTalkApprovalDispatchQuery: _q}
	sbuild.label = dingtalkapprovaldispatch.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DingTalkApprovalDispatchSelect configured with the given aggregations.
func (_q *DingTalkApprovalDispatchQuery) Aggregate(fns ...AggregateFunc) *DingTalkApprovalDispatchSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *DingTalkApprovalDispatchQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !dingtalkapprovaldispatch.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *DingTalkApprovalDispatchQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DingTalkApprovalDispatch, error) {
	var (
		nodes       = []*DingTalkApprovalDispatch{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withOrganization != nil,
			_q.withBackgroundTask != nil,
			_q.withUnlockRequest != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DingTalkApprovalDispatch).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DingTalkApprovalDispatch{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *DingTalkApprovalDispatch, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBackgroundTask; query != nil {
		if err := _q.loadBackgroundTask(ctx, query, nodes, nil,
			func(n *DingTalkApprovalDispatch, e *BackgroundTask) { n.Edges.BackgroundTask = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUnlockRequest; query != nil {
		if err := _q.loadUnlockRequest(ctx, query, nodes, nil,
			func(n *DingTalkApprovalDispatch, e *OrderUnlockRequest) { n.Edges.UnlockRequest = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *DingTalkApprovalDispatchQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*DingTalkApprovalDispatch, init func(*DingTalkApprovalDispatch), assign func(*DingTalkApprovalDispatch, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DingTalkApprovalDispatch)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DingTalkApprovalDispatchQuery) loadBackgroundTask(ctx context.Context, query *BackgroundTaskQuery, nodes []*DingTalkApprovalDispatch, init func(*DingTalkApprovalDispatch), assign func(*DingTalkApprovalDispatch, *BackgroundTask)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DingTalkApprovalDispatch)
	for i := range nodes {
		fk := nodes[i].BackgroundTaskID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(backgroundtask.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "background_task_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DingTalkApprovalDispatchQuery) loadUnlockRequest(ctx context.Context, query *OrderUnlockRequestQuery, nodes []*DingTalkApprovalDispatch, init func(*DingTalkApprovalDispatch), assign func(*DingTalkApprovalDispatch, *OrderUnlockRequest)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DingTalkApprovalDispatch)
	for i := range nodes {
		fk := nodes[i].UnlockRequestID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderunlockrequest.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "unlock_request_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *DingTalkApprovalDispatchQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *DingTalkApprovalDispatchQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dingtalkapprovaldispatch.Table, dingtalkapprovaldispatch.Columns, sqlgraph.NewFieldSpec(dingtalkapprovaldispatch.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dingtalkapprovaldispatch.FieldID)
		for i := range fields {
			if fields[i] != dingtalkapprovaldispatch.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(dingtalkapprovaldispatch.FieldOrganizationID)
		}
		if _q.withBackgroundTask != nil {
			_spec.Node.AddColumnOnce(dingtalkapprovaldispatch.FieldBackgroundTaskID)
		}
		if _q.withUnlockRequest != nil {
			_spec.Node.AddColumnOnce(dingtalkapprovaldispatch.FieldUnlockRequestID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *DingTalkApprovalDispatchQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(dingtalkapprovaldispatch.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = dingtalkapprovaldispatch.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *DingTalkApprovalDispatchQuery) ForUpdate(opts ...sql.LockOption) *DingTalkApprovalDispatchQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *DingTalkApprovalDispatchQuery) ForShare(opts ...sql.LockOption) *DingTalkApprovalDispatchQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// DingTalkApprovalDispatchGroupBy is the group-by builder for DingTalkApprovalDispatch entities.
type DingTalkApprovalDispatchGroupBy struct {
	selector
	build *DingTalkApprovalDispatchQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *DingTalkApprovalDispatchGroupBy) Aggregate(fns ...AggregateFunc) *DingTalkApprovalDispatchGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *DingTalkApprovalDispatchGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DingTalkApprovalDispatchQuery, *DingTalkApprovalDispatchGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *DingTalkApprovalDispatchGroupBy) sqlScan(ctx context.Context, root *DingTalkApprovalDispatchQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DingTalkApprovalDispatchSelect is the builder for selecting fields of DingTalkApprovalDispatch entities.
type DingTalkApprovalDispatchSelect struct {
	*DingTalkApprovalDispatchQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *DingTalkApprovalDispatchSelect) Aggregate(fns ...AggregateFunc) *DingTalkApprovalDispatchSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *DingTalkApprovalDispatchSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DingTalkApprovalDispatchQuery, *DingTalkApprovalDispatchSelect](ctx, _s.DingTalkApprovalDispatchQuery, _s, _s.inters, v)
}

func (_s *DingTalkApprovalDispatchSelect) sqlScan(ctx context.Context, root *DingTalkApprovalDispatchQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
