// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/backgroundtask"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/objectstoragedeletion"
)

// ObjectStorageDeletion is the model entity for the ObjectStorageDeletion schema.
type ObjectStorageDeletion struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// BackgroundTaskID holds the value of the "background_task_id" field.
	BackgroundTaskID uuid.UUID `json:"background_task_id,omitempty"`
	// ObjectKey holds the value of the "object_key" field.
	ObjectKey string `json:"object_key,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ObjectStorageDeletionQuery when eager-loading is set.
	Edges        ObjectStorageDeletionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ObjectStorageDeletionEdges holds the relations/edges for other nodes in the graph.
type ObjectStorageDeletionEdges struct {
	// BackgroundTask holds the value of the background_task edge.
	BackgroundTask *BackgroundTask `json:"background_task,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BackgroundTaskOrErr returns the BackgroundTask value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ObjectStorageDeletionEdges) BackgroundTaskOrErr() (*BackgroundTask, error) {
	if e.BackgroundTask != nil {
		return e.BackgroundTask, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: backgroundtask.Label}
	}
	return nil, &NotLoadedError{edge: "background_task"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ObjectStorageDeletion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case objectstoragedeletion.FieldObjectKey:
			values[i] = new(sql.NullString)
		case objectstoragedeletion.FieldCreatedAt, objectstoragedeletion.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case objectstoragedeletion.FieldID, objectstoragedeletion.FieldBackgroundTaskID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ObjectStorageDeletion fields.
func (_m *ObjectStorageDeletion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case objectstoragedeletion.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case objectstoragedeletion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case objectstoragedeletion.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case objectstoragedeletion.FieldBackgroundTaskID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field background_task_id", values[i])
			} else if value != nil {
				_m.BackgroundTaskID = *value
			}
		case objectstoragedeletion.FieldObjectKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field object_key", values[i])
			} else if value.Valid {
				_m.ObjectKey = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ObjectStorageDeletion.
// This includes values selected through modifiers, order, etc.
func (_m *ObjectStorageDeletion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBackgroundTask queries the "background_task" edge of the ObjectStorageDeletion entity.
func (_m *ObjectStorageDeletion) QueryBackgroundTask() *BackgroundTaskQuery {
	return NewObjectStorageDeletionClient(_m.config).QueryBackgroundTask(_m)
}

// Update returns a builder for updating this ObjectStorageDeletion.
// Note that you need to call ObjectStorageDeletion.Unwrap() before calling this method if this ObjectStorageDeletion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ObjectStorageDeletion) Update() *ObjectStorageDeletionUpdateOne {
	return NewObjectStorageDeletionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ObjectStorageDeletion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ObjectStorageDeletion) Unwrap() *ObjectStorageDeletion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ObjectStorageDeletion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ObjectStorageDeletion) String() string {
	var builder strings.Builder
	builder.WriteString("ObjectStorageDeletion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("background_task_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.BackgroundTaskID))
	builder.WriteString(", ")
	builder.WriteString("object_key=")
	builder.WriteString(_m.ObjectKey)
	builder.WriteByte(')')
	return builder.String()
}

// ObjectStorageDeletions is a parsable slice of ObjectStorageDeletion.
type ObjectStorageDeletions []*ObjectStorageDeletion
