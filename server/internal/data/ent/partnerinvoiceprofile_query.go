// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/financeinvoice"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/organization"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/partner"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/partnerinvoiceprofile"
	"github.com/roncin/roncin-go-admin/server/internal/data/ent/predicate"
)

// PartnerInvoiceProfileQuery is the builder for querying PartnerInvoiceProfile entities.
type PartnerInvoiceProfileQuery struct {
	config
	ctx                 *QueryContext
	order               []partnerinvoiceprofile.OrderOption
	inters              []Interceptor
	predicates          []predicate.PartnerInvoiceProfile
	withOrganization    *OrganizationQuery
	withPartner         *PartnerQuery
	withFinanceInvoices *FinanceInvoiceQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartnerInvoiceProfileQuery builder.
func (_q *PartnerInvoiceProfileQuery) Where(ps ...predicate.PartnerInvoiceProfile) *PartnerInvoiceProfileQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PartnerInvoiceProfileQuery) Limit(limit int) *PartnerInvoiceProfileQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PartnerInvoiceProfileQuery) Offset(offset int) *PartnerInvoiceProfileQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PartnerInvoiceProfileQuery) Unique(unique bool) *PartnerInvoiceProfileQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PartnerInvoiceProfileQuery) Order(o ...partnerinvoiceprofile.OrderOption) *PartnerInvoiceProfileQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *PartnerInvoiceProfileQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partnerinvoiceprofile.Table, partnerinvoiceprofile.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partnerinvoiceprofile.OrganizationTable, partnerinvoiceprofile.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPartner chains the current query on the "partner" edge.
func (_q *PartnerInvoiceProfileQuery) QueryPartner() *PartnerQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partnerinvoiceprofile.Table, partnerinvoiceprofile.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, partnerinvoiceprofile.PartnerTable, partnerinvoiceprofile.PartnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFinanceInvoices chains the current query on the "finance_invoices" edge.
func (_q *PartnerInvoiceProfileQuery) QueryFinanceInvoices() *FinanceInvoiceQuery {
	query := (&FinanceInvoiceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partnerinvoiceprofile.Table, partnerinvoiceprofile.FieldID, selector),
			sqlgraph.To(financeinvoice.Table, financeinvoice.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, partnerinvoiceprofile.FinanceInvoicesTable, partnerinvoiceprofile.FinanceInvoicesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PartnerInvoiceProfile entity from the query.
// Returns a *NotFoundError when no PartnerInvoiceProfile was found.
func (_q *PartnerInvoiceProfileQuery) First(ctx context.Context) (*PartnerInvoiceProfile, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{partnerinvoiceprofile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) FirstX(ctx context.Context) *PartnerInvoiceProfile {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PartnerInvoiceProfile ID from the query.
// Returns a *NotFoundError when no PartnerInvoiceProfile ID was found.
func (_q *PartnerInvoiceProfileQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{partnerinvoiceprofile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PartnerInvoiceProfile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PartnerInvoiceProfile entity is found.
// Returns a *NotFoundError when no PartnerInvoiceProfile entities are found.
func (_q *PartnerInvoiceProfileQuery) Only(ctx context.Context) (*PartnerInvoiceProfile, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{partnerinvoiceprofile.Label}
	default:
		return nil, &NotSingularError{partnerinvoiceprofile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) OnlyX(ctx context.Context) *PartnerInvoiceProfile {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PartnerInvoiceProfile ID in the query.
// Returns a *NotSingularError when more than one PartnerInvoiceProfile ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PartnerInvoiceProfileQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{partnerinvoiceprofile.Label}
	default:
		err = &NotSingularError{partnerinvoiceprofile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PartnerInvoiceProfiles.
func (_q *PartnerInvoiceProfileQuery) All(ctx context.Context) ([]*PartnerInvoiceProfile, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PartnerInvoiceProfile, *PartnerInvoiceProfileQuery]()
	return withInterceptors[[]*PartnerInvoiceProfile](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) AllX(ctx context.Context) []*PartnerInvoiceProfile {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PartnerInvoiceProfile IDs.
func (_q *PartnerInvoiceProfileQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(partnerinvoiceprofile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PartnerInvoiceProfileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PartnerInvoiceProfileQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PartnerInvoiceProfileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PartnerInvoiceProfileQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartnerInvoiceProfileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PartnerInvoiceProfileQuery) Clone() *PartnerInvoiceProfileQuery {
	if _q == nil {
		return nil
	}
	return &PartnerInvoiceProfileQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]partnerinvoiceprofile.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.PartnerInvoiceProfile{}, _q.predicates...),
		withOrganization:    _q.withOrganization.Clone(),
		withPartner:         _q.withPartner.Clone(),
		withFinanceInvoices: _q.withFinanceInvoices.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PartnerInvoiceProfileQuery) WithOrganization(opts ...func(*OrganizationQuery)) *PartnerInvoiceProfileQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithPartner tells the query-builder to eager-load the nodes that are connected to
// the "partner" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PartnerInvoiceProfileQuery) WithPartner(opts ...func(*PartnerQuery)) *PartnerInvoiceProfileQuery {
	query := (&PartnerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPartner = query
	return _q
}

// WithFinanceInvoices tells the query-builder to eager-load the nodes that are connected to
// the "finance_invoices" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PartnerInvoiceProfileQuery) WithFinanceInvoices(opts ...func(*FinanceInvoiceQuery)) *PartnerInvoiceProfileQuery {
	query := (&FinanceInvoiceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFinanceInvoices = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PartnerInvoiceProfile.Query().
//		GroupBy(partnerinvoiceprofile.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PartnerInvoiceProfileQuery) GroupBy(field string, fields ...string) *PartnerInvoiceProfileGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PartnerInvoiceProfileGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = partnerinvoiceprofile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PartnerInvoiceProfile.Query().
//		Select(partnerinvoiceprofile.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *PartnerInvoiceProfileQuery) Select(fields ...string) *PartnerInvoiceProfileSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PartnerInvoiceProfileSelect{PartnerInvoiceProfileQuery: _q}
	sbuild.label = partnerinvoiceprofile.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PartnerInvoiceProfileSelect configured with the given aggregations.
func (_q *PartnerInvoiceProfileQuery) Aggregate(fns ...AggregateFunc) *PartnerInvoiceProfileSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PartnerInvoiceProfileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !partnerinvoiceprofile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PartnerInvoiceProfileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PartnerInvoiceProfile, error) {
	var (
		nodes       = []*PartnerInvoiceProfile{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withOrganization != nil,
			_q.withPartner != nil,
			_q.withFinanceInvoices != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PartnerInvoiceProfile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PartnerInvoiceProfile{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *PartnerInvoiceProfile, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPartner; query != nil {
		if err := _q.loadPartner(ctx, query, nodes, nil,
			func(n *PartnerInvoiceProfile, e *Partner) { n.Edges.Partner = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFinanceInvoices; query != nil {
		if err := _q.loadFinanceInvoices(ctx, query, nodes,
			func(n *PartnerInvoiceProfile) { n.Edges.FinanceInvoices = []*FinanceInvoice{} },
			func(n *PartnerInvoiceProfile, e *FinanceInvoice) {
				n.Edges.FinanceInvoices = append(n.Edges.FinanceInvoices, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PartnerInvoiceProfileQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*PartnerInvoiceProfile, init func(*PartnerInvoiceProfile), assign func(*PartnerInvoiceProfile, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*PartnerInvoiceProfile)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PartnerInvoiceProfileQuery) loadPartner(ctx context.Context, query *PartnerQuery, nodes []*PartnerInvoiceProfile, init func(*PartnerInvoiceProfile), assign func(*PartnerInvoiceProfile, *Partner)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*PartnerInvoiceProfile)
	for i := range nodes {
		fk := nodes[i].PartnerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "partner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PartnerInvoiceProfileQuery) loadFinanceInvoices(ctx context.Context, query *FinanceInvoiceQuery, nodes []*PartnerInvoiceProfile, init func(*PartnerInvoiceProfile), assign func(*PartnerInvoiceProfile, *FinanceInvoice)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*PartnerInvoiceProfile)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(financeinvoice.FieldInvoiceProfileID)
	}
	query.Where(predicate.FinanceInvoice(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(partnerinvoiceprofile.FinanceInvoicesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.InvoiceProfileID
		if fk == nil {
			return fmt.Errorf(`foreign-key "invoice_profile_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "invoice_profile_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PartnerInvoiceProfileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PartnerInvoiceProfileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(partnerinvoiceprofile.Table, partnerinvoiceprofile.Columns, sqlgraph.NewFieldSpec(partnerinvoiceprofile.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, partnerinvoiceprofile.FieldID)
		for i := range fields {
			if fields[i] != partnerinvoiceprofile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(partnerinvoiceprofile.FieldOrganizationID)
		}
		if _q.withPartner != nil {
			_spec.Node.AddColumnOnce(partnerinvoiceprofile.FieldPartnerID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PartnerInvoiceProfileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(partnerinvoiceprofile.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = partnerinvoiceprofile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *PartnerInvoiceProfileQuery) ForUpdate(opts ...sql.LockOption) *PartnerInvoiceProfileQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *PartnerInvoiceProfileQuery) ForShare(opts ...sql.LockOption) *PartnerInvoiceProfileQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// PartnerInvoiceProfileGroupBy is the group-by builder for PartnerInvoiceProfile entities.
type PartnerInvoiceProfileGroupBy struct {
	selector
	build *PartnerInvoiceProfileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PartnerInvoiceProfileGroupBy) Aggregate(fns ...AggregateFunc) *PartnerInvoiceProfileGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PartnerInvoiceProfileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartnerInvoiceProfileQuery, *PartnerInvoiceProfileGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PartnerInvoiceProfileGroupBy) sqlScan(ctx context.Context, root *PartnerInvoiceProfileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PartnerInvoiceProfileSelect is the builder for selecting fields of PartnerInvoiceProfile entities.
type PartnerInvoiceProfileSelect struct {
	*PartnerInvoiceProfileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PartnerInvoiceProfileSelect) Aggregate(fns ...AggregateFunc) *PartnerInvoiceProfileSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PartnerInvoiceProfileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartnerInvoiceProfileQuery, *PartnerInvoiceProfileSelect](ctx, _s.PartnerInvoiceProfileQuery, _s, _s.inters, v)
}

func (_s *PartnerInvoiceProfileSelect) sqlScan(ctx context.Context, root *PartnerInvoiceProfileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
